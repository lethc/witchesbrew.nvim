-- local lush = require("lush")
local theme = require("witchesbrew.theme")

-- A placeholder to add configuration keeping backwards compatibility.
local function setup()
  return theme
end

return { setup = setup }
